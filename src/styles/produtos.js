import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
	container: {
		padding: 20,
	},

	areaTocavel: {
	},

	text: {
		color: '#000000',
		fontWeight: 'bold',
		fontSize: 20
	},

	comprarButton: {
		color: '#e9967a',
		fontSize: 20
	}
})

export default styles